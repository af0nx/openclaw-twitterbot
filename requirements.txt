# Twitter Bot Pipeline V2 Dependencies
# Python 3.11+ required
# Versions pinned to compatible ranges for reproducible builds

# Core Frameworks
scrapling>=0.2.0,<0.3.0          # God-mode scraping with anti-detection
openai>=1.12.0,<2.0.0            # OpenRouter integration (OpenAI SDK compatible)
psycopg2-binary>=2.9.9,<3.0.0    # PostgreSQL adapter
python-telegram-bot>=20.7,<21.0  # Telegram HITL gateway
praw>=7.7.1,<8.0.0               # Reddit API wrapper

# Data Processing
feedparser>=6.0.10,<7.0.0        # RSS parsing
beautifulsoup4>=4.12.0,<5.0.0    # HTML parsing fallback
lxml>=5.1.0,<6.0.0               # Fast XML/HTML parser
pillow>=10.2.0,<11.0.0           # Image processing for Siftly
pytesseract>=0.3.10,<0.4.0       # OCR bridge for Siftly and live-action detection
opencv-python-headless>=4.10.0,<5.0.0  # Local live-action detection
requests>=2.31.0,<3.0.0          # HTTP client
httpx>=0.26.0,<1.0.0             # Async HTTP client

# ML/NLP
numpy>=1.26.0,<2.0.0             # Numerical computing
torch>=2.2.0,<3.0.0              # PyTorch for Siftly vision models
transformers>=4.37.0,<5.0.0      # HuggingFace models
sentence-transformers>=2.3.1,<3.0.0  # Embeddings for semantic dedup + drift ceiling
scikit-learn>=1.4.0,<2.0.0       # Persona classifier + tone classifier (TF-IDF/SVM)

# Utilities
python-dotenv>=1.0.0,<2.0.0      # Environment management
tenacity>=8.2.3,<9.0.0           # Retry logic
pytz>=2024.1                     # Timezone handling (no upper pin — timezone data)
jsonschema>=4.21.0,<5.0.0        # JSON validation
pyyaml>=6.0.1,<7.0.0             # YAML config parser
colorlog>=6.8.2,<7.0.0           # Colored logging

# Security
cryptography>=42.0.0,<44.0.0     # Encryption utilities

# Monitoring & DevOps
sentry-sdk>=1.40.0,<3.0.0        # Error tracking
prometheus-client>=0.19.0,<1.0.0  # Metrics export
playwright>=1.48.0,<2.0.0        # Twitch stream capture
tweepy>=4.14.0,<5.0.0            # X posting + media upload

# Testing (optional)
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.4,<1.0.0
bandit>=1.7.7,<2.0.0             # Security linter
semgrep>=1.59.0,<2.0.0           # SAST scanner
